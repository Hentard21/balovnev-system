// ═════════════════════════════════════════════════════════════════════════════
// ЦЕНТРАЛЬНЫЙ КОНФИГ САЙТА
// Все контакты, ссылки и настройки в одном месте — чтобы при запуске
// заменить заглушки одним заходом в этот файл.
// ═════════════════════════════════════════════════════════════════════════════

// ─── Базовый путь (на случай возврата на github.io/<repo>) ──────────────────
// Сайт живёт на собственном домене balovnev-system.ru и отдаётся с корня,
// поэтому basePath сейчас пуст. Все ссылки на статику всё равно идут через
// asset() — если домен отвяжут и сайт вернётся под github.io/balovnev-system,
// достаточно будет задать NEXT_PUBLIC_BASE_PATH в workflow.
export const BASE_PATH = process.env.NEXT_PUBLIC_BASE_PATH ?? "";

/** Префиксует путь к статическому файлу текущим basePath (обычно пустым). */
export function asset(path: string): string {
  return `${BASE_PATH}${path}`;
}

// ─── Публичный адрес сайта ───────────────────────────────────────────────────
export const SITE_URL = "https://balovnev-system.ru";

// ─── Контакты и соцсети ──────────────────────────────────────────────────────
// Реальные ссылки Игоря. Пустая строка = канал не используется (скрывается).
export const CONTACTS = {
  telegram: "https://t.me/BalovnevFitness",
  instagram: "https://instagram.com/balovnev_fitness",
  whatsapp: "", // WhatsApp у тренера нет — ссылка скрыта
  vk: "https://vk.com/id430514648",
};

// ─── Приём заявок с формы ────────────────────────────────────────────────────
// Обработчик на Yandex Cloud Functions: functions/yandex/lead/index.js. Он
// присылает карточку заявки тренеру в Telegram — хранилища нет, входящие
// заявки это переписка с ботом.
//
// Сайт статический (GitHub Pages, коммерческое использование там официально
// запрещено — переезд на Cloudflare Pages не удался, оттуда без VPN сайт не
// открывался из России), а функция живёт на functions.yandexcloud.net — это
// чужой домен, поэтому у неё настроен CORS только под этот сайт (см. код
// функции) и обработка preflight-запроса OPTIONS.
//
// Токен бота и chat id тренера — переменные окружения самой функции в
// консоли Yandex Cloud, в репозиторий не попадают. Функция сделана публичной
// (system:allUsers → functions.functionInvoker), иначе HTTP-вызов без
// авторизации получает отказ.
export const FORM_ENDPOINT = "https://functions.yandexcloud.net/d4ep5jc7c4l86e7djrs5";

// Запасной канал на почту через Formspree — временно выключен намеренно:
// Yandex Cloud Functions сейчас нестабильно достаёт до api.telegram.org
// (стабильно виснет на таймауте функции при части вызовов), и с включённым
// откатом эти отказы тихо уходили на почту вместо Telegram, маскируя
// проблему. Пока разбираемся — пусть отказ основного канала будет виден:
// форма покажет экран прямой связи вместо ложного «отправлено».
// Адрес Formspree: https://formspree.io/f/xppakqry — вернуть сюда, когда
// нужно снова включить откат.
export const FORM_ENDPOINT_FALLBACK = "";

// ─── Аналитика ───────────────────────────────────────────────────────────────
// TODO: создайте счётчик на https://metrika.yandex.ru и впишите его номер.
// Пока пусто — счётчик не подключается.
export const YANDEX_METRIKA_ID = "111759825";

// ─── Скидка ──────────────────────────────────────────────────────────────────
export const DISCOUNT_PERCENT = 25;
