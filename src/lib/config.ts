// ═════════════════════════════════════════════════════════════════════════════
// ЦЕНТРАЛЬНЫЙ КОНФИГ САЙТА
// Все контакты, ссылки и настройки в одном месте — чтобы при запуске
// заменить заглушки одним заходом в этот файл.
// ═════════════════════════════════════════════════════════════════════════════

// ─── Базовый путь (на случай возврата на github.io/<repo>) ──────────────────
// Сайт живёт на собственном домене balovnev-system.ru и отдаётся с корня,
// поэтому basePath сейчас пуст. Все ссылки на статику всё равно идут через
// asset() — если домен отвяжут и сайт вернётся под github.io/balovnev-system,
// достаточно будет задать NEXT_PUBLIC_BASE_PATH в workflow.
export const BASE_PATH = process.env.NEXT_PUBLIC_BASE_PATH ?? "";

/** Префиксует путь к статическому файлу текущим basePath (обычно пустым). */
export function asset(path: string): string {
  return `${BASE_PATH}${path}`;
}

// ─── Публичный адрес сайта ───────────────────────────────────────────────────
export const SITE_URL = "https://balovnev-system.ru";

// ─── Контакты и соцсети ──────────────────────────────────────────────────────
// Реальные ссылки Игоря. Пустая строка = канал не используется (скрывается).
export const CONTACTS = {
  telegram: "https://t.me/BalovnevFitness",
  instagram: "https://instagram.com/balovnev_fitness",
  whatsapp: "", // WhatsApp у тренера нет — ссылка скрыта
  vk: "https://vk.com/id430514648",
};

// ─── Приём заявок с формы ────────────────────────────────────────────────────
// Основной канал — Telegram, причём отправка идёт ИЗ БРАУЗЕРА клиента
// (см. src/lib/lead-telegram.ts, там же разбор рисков этого решения).
//
// Путь через свой серверный обработчик пробовали дважды и оба раза упёрлись
// в инфраструктуру: Cloudflare Pages не открывался из России без VPN, а
// Yandex Cloud Functions нестабильно достаёт до api.telegram.org — замер дал
// 5 успехов из 12, ретраи не помогали. У клиента Telegram работает, поэтому
// отправка перенесена на его сторону.
//
// ВНИМАНИЕ: токен ниже публичный — он попадает в исходники страницы, спрятать
// его невозможно. Это осознанный размен, он безопасен ровно до тех пор, пока
// бот занимается только доставкой заявок тренеру. Через токен нельзя прочитать
// анкеты клиентов и нельзя писать никому, кроме тех, кто сам нажал у бота
// Start. Мини-аппу нужен ОТДЕЛЬНЫЙ бот — этому расширять полномочия нельзя.
// Скомпрометируют — /revoke у @BotFather и новый токен сюда.
export const TELEGRAM_BOT_TOKEN = "7753176190:AAGPG9C2U4v36yYGsFNjARbGnCMU4Ol41EE";

// Telegram id тренера (@balovnev_igor). Он должен был хотя бы раз нажать
// Start у бота — иначе Telegram откажет с «chat not found».
export const TRAINER_CHAT_ID = "1637859959";

// Запасной канал на почту: если отправка в Telegram не прошла (у клиента
// заблокирован api.telegram.org, нет сети, бот забанен), заявка уходит через
// Formspree. Инфраструктура другая, поэтому одновременный отказ обоих каналов
// маловероятен. Пустая строка отключает запасной путь.
export const FORM_ENDPOINT_FALLBACK = "https://formspree.io/f/xppakqry";

// ─── Аналитика ───────────────────────────────────────────────────────────────
// TODO: создайте счётчик на https://metrika.yandex.ru и впишите его номер.
// Пока пусто — счётчик не подключается.
export const YANDEX_METRIKA_ID = "111759825";

// ─── Скидка ──────────────────────────────────────────────────────────────────
export const DISCOUNT_PERCENT = 25;
