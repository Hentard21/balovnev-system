// ═════════════════════════════════════════════════════════════════════════════
// ЦЕНТРАЛЬНЫЙ КОНФИГ САЙТА
// Все контакты, ссылки и настройки в одном месте — чтобы при запуске
// заменить заглушки одним заходом в этот файл.
// ═════════════════════════════════════════════════════════════════════════════

// ─── Базовый путь (GitHub Pages) ─────────────────────────────────────────────
// На GitHub Pages сайт живёт в подпапке /balovnev-system, поэтому все ссылки
// на статические файлы (фото, видео) должны идти через asset().
export const BASE_PATH = process.env.NEXT_PUBLIC_BASE_PATH ?? "";

/** Префиксует путь к статическому файлу basePath'ом GitHub Pages. */
export function asset(path: string): string {
  return `${BASE_PATH}${path}`;
}

// ─── Публичный адрес сайта ───────────────────────────────────────────────────
// TODO: замените <username> на реальный аккаунт GitHub (или на свой домен).
export const SITE_URL = "https://USERNAME.github.io/balovnev-system";

// ─── Контакты и соцсети ──────────────────────────────────────────────────────
// TODO: заменить все ссылки на реальные перед запуском.
export const CONTACTS = {
  telegram: "https://t.me/balovnev", // ← заменить на реальный username
  instagram: "https://instagram.com/balovnev", // ← заменить
  whatsapp: "https://wa.me/79000000000", // ← заменить на реальный номер
  vk: "https://vk.com/balovnev", // ← заменить
};

// ─── Приём заявок с формы ────────────────────────────────────────────────────
// Сайт статический (GitHub Pages), поэтому форме нужен внешний endpoint.
// Варианты (любой из):
//   1. Вебхук вашего Telegram-бота (Cloudflare Worker / VPS), принимающий
//      POST JSON {name, contact, goal} и пересылающий сообщение тренеру.
//   2. Formspree/Formcarry: создайте форму и вставьте их URL сюда.
// Пока endpoint пустой — форма показывает кнопки прямой связи (Telegram/WhatsApp).
export const FORM_ENDPOINT = ""; // ← вставить URL вебхука, когда бот будет готов

// ─── Аналитика ───────────────────────────────────────────────────────────────
// TODO: создайте счётчик на https://metrika.yandex.ru и впишите его номер.
// Пока пусто — счётчик не подключается.
export const YANDEX_METRIKA_ID = ""; // например "99999999"

// ─── Скидка ──────────────────────────────────────────────────────────────────
export const DISCOUNT_PERCENT = 25;
